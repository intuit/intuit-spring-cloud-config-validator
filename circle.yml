machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - pip install -r requirements.txt

test:
  pre:
    - pip install coverage
  override:
    - coverage run -m unittest discover -v tests  
  post:
    - mv html_cov $CIRCLE_ARTIFACTS/coverage_html
